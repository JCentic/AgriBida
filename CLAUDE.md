@AGENTS.md

## Claude Code-specific notes

- This is a static prototype: no `package.json`, no build tool, no test runner. Do not search for or invent npm/build scripts — open the HTML files directly to validate changes.
- There is no automated test suite. Validate changed flows manually against the relevant cases in `plans/07-agribida-test-plan.md` and report which cases were checked.
